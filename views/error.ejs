<% layout("./layouts/boilerplate.ejs") %>

<div class="row">
    <div class="alert alert-danger col-6 offset-3" role="alert">
        <p class="alert-heading"><%= message%> </p>
        <!-- <p><%= err.trace%></p>
        <p><%= err.stack%></p>  specify to know the error from server(server host)-->
    </div>
</div>

